# This script collects all header elements
# of the main model. This is most like
# going to be decrecated in future updates.

model1UI_header <- function(id, output){
  # 
  # ns <- NS(id)
  # 
  # tagList(
  #   
  #   tags$li(
  #     (
  #       dropdownButton(
  #         label = "Effektforventinger",circle = FALSE,
  #         tooltip = "Vælg Årlig Effekt",
  #         map(
  #           1:5,
  #           .f = function(i) {
  #             
  #             sliderInput(
  #               inputId = ns(paste0("effect_", i)),
  #               label = paste("Tid", i),
  #               min = 0,
  #               max = 100,
  #               value = round(runif(1, min = 0, max = 100))
  #             )
  #           }
  #         )
  #       )
  #       
  #     )
  #     ,
  #     class = "dropdown"
  #   )
  #   
  # )
  
}
